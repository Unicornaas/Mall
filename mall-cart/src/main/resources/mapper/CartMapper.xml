<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.fjut.mall.cart.mapper.CartMapper">
    <resultMap id="BaseResultMap" type="edu.fjut.mall.cart.entity.Cart">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="sku_id" property="skuId"/>
        <result column="quantity" property="quantity"/>
        <result column="selected" property="selected"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert">
        INSERT INTO cart (id, user_id, sku_id, quantity, selected)
        VALUES (#{id}, #{userId}, #{skuId}, #{quantity}, #{selected})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE id = #{id}
    </select>

    <select id="selectByUserAndSku" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE user_id = #{userId} AND sku_id = #{skuId} LIMIT 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM cart WHERE user_id = #{userId} ORDER BY id DESC
    </select>

    <update id="updateQuantity">
        UPDATE cart SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <update id="updateSelected">
        UPDATE cart SET selected = #{selected} WHERE id = #{id}
    </update>

    <update id="selectAll">
        UPDATE cart SET selected = #{selected} WHERE user_id = #{userId}
    </update>

    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <delete id="deleteBatch">
        DELETE FROM cart WHERE id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
        AND user_id = #{userId}
    </delete>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM cart WHERE user_id = #{userId}
    </select>

    <delete id="deleteByUserIdAndIds">
        DELETE FROM cart WHERE user_id = #{userId} AND id IN
        <foreach collection="ids" item="id" open="(" close=")" separator=",">
            #{id}
        </foreach>
    </delete>
</mapper>
