
# 1. 进入前端目录
cd D:\实训\商城系统\Mall\mall-front

# 2. 安装依赖（如果还没装过）
npm install

# 3. 启动开发服务器
npm run dev

启动后访问 http://localhost:5173 即可。

1. 打开 cmd / PowerShell，切换到 bin 目录
cmd
cd D:\nacos\bin
2. 单机模式启动（最常用，开发本地）
   cmd
   startup.cmd -m standalone