<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.fjut.mall.product.mapper.ProductSpuMapper">

    <resultMap id="BaseResultMap" type="edu.fjut.mall.product.entity.ProductSpu">
        <id column="id" property="id"/>
        <result column="seller_id" property="sellerId"/>
        <result column="shop_name" property="shopName"/>
        <result column="category_id" property="categoryId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="brand" property="brand"/>
        <result column="main_image" property="mainImage"/>
        <result column="images" property="images"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByCategoryId" resultMap="BaseResultMap">
        SELECT spu.*, COALESCE(shop.shop_name, '平台自营') AS shop_name
        FROM product_spu spu
        LEFT JOIN shop_t shop ON shop.seller_id = spu.seller_id
        WHERE spu.category_id = #{categoryId}
        ORDER BY spu.id DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT spu.*, COALESCE(shop.shop_name, '平台自营') AS shop_name
        FROM product_spu spu
        LEFT JOIN shop_t shop ON shop.seller_id = spu.seller_id
        WHERE spu.id = #{id}
    </select>

    <select id="selectByIdAndSellerId" resultMap="BaseResultMap">
        SELECT spu.*, COALESCE(shop.shop_name, '平台自营') AS shop_name
        FROM product_spu spu
        LEFT JOIN shop_t shop ON shop.seller_id = spu.seller_id
        WHERE spu.id = #{id} AND spu.seller_id = #{sellerId}
    </select>

    <insert id="insert">
        INSERT INTO product_spu (id, seller_id, category_id, name, description, brand, main_image, images, status)
        VALUES (#{id}, #{sellerId}, #{categoryId}, #{name}, #{description}, #{brand}, #{mainImage}, #{images}, #{status})
    </insert>

    <update id="updateById">
        UPDATE product_spu
        <set>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="brand != null">brand = #{brand},</if>
            <if test="mainImage != null">main_image = #{mainImage},</if>
            <if test="images != null">images = #{images},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE product_spu SET status = #{status} WHERE id = #{id}
    </update>

    <select id="selectPageForAdmin" resultMap="BaseResultMap">
        SELECT * FROM product_spu
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR brand LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="categoryId != null">AND category_id = #{categoryId}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countForAdmin" resultType="long">
        SELECT COUNT(*) FROM product_spu
        <where>
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR brand LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="categoryId != null">AND category_id = #{categoryId}</if>
            <if test="status != null">AND status = #{status}</if>
        </where>
    </select>

    <sql id="SellerProductWhere">
        <where>
            spu.seller_id = #{sellerId}
            <if test="query.keyword != null and query.keyword != ''">
                AND (spu.name LIKE CONCAT('%', #{query.keyword}, '%')
                    OR spu.brand LIKE CONCAT('%', #{query.keyword}, '%'))
            </if>
            <if test="query.categoryId != null">AND spu.category_id = #{query.categoryId}</if>
            <if test="query.status != null">AND spu.status = #{query.status}</if>
        </where>
    </sql>

    <select id="selectPageForSeller" resultMap="BaseResultMap">
        SELECT spu.*, COALESCE(shop.shop_name, '平台自营') AS shop_name
        FROM product_spu spu
        LEFT JOIN shop_t shop ON shop.seller_id = spu.seller_id
        <include refid="SellerProductWhere"/>
        ORDER BY spu.create_time DESC
        LIMIT #{query.offset}, #{query.pageSize}
    </select>

    <select id="countForSeller" resultType="long">
        SELECT COUNT(*) FROM product_spu spu
        <include refid="SellerProductWhere"/>
    </select>
</mapper>
