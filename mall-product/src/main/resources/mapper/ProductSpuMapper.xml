<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.fjut.mall.product.mapper.ProductSpuMapper">

    <resultMap id="BaseResultMap" type="edu.fjut.mall.product.entity.ProductSpu">
        <id column="id" property="id"/>
        <result column="category_id" property="categoryId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="brand" property="brand"/>
        <result column="main_image" property="mainImage"/>
        <result column="images" property="images"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByCategoryId" resultMap="BaseResultMap">
        SELECT * FROM product_spu WHERE category_id = #{categoryId} ORDER BY id DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM product_spu WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO product_spu (id, category_id, name, description, brand, main_image, images, status)
        VALUES (#{id}, #{categoryId}, #{name}, #{description}, #{brand}, #{mainImage}, #{images}, #{status})
    </insert>

    <update id="updateById">
        UPDATE product_spu
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="brand != null">brand = #{brand},</if>
            <if test="mainImage != null">main_image = #{mainImage},</if>
            <if test="images != null">images = #{images},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE product_spu SET status = #{status} WHERE id = #{id}
    </update>
</mapper>
