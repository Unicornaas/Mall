<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.fjut.mall.product.mapper.ProductCategoryMapper">

    <resultMap id="BaseResultMap" type="edu.fjut.mall.product.entity.ProductCategory">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="name" property="name"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="icon" property="icon"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM product_category ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT * FROM product_category WHERE parent_id = #{parentId} ORDER BY sort_order ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM product_category WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO product_category (id, parent_id, name, sort_order, icon, status)
        VALUES (#{id}, #{parentId}, #{name}, #{sortOrder}, #{icon}, #{status})
    </insert>

    <update id="updateById">
        UPDATE product_category
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM product_category WHERE id = #{id}
    </delete>
</mapper>
